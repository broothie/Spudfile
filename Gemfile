source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in spud.gemspec
gemspec

group :development do
  gem "pry", "~> 0.13.1"
  gem "byebug", "~> 11.0"
end
